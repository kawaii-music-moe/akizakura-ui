# Shape

形状（角丸など）に関するトークン。CSS 変数として `:root` に定義する。

## 角丸

| トークン | 値 | 用途 |
|---|---|---|
| `--radius` | `0.5rem` | 既定の角丸（ボタン・入力欄など） |
| `--radius-lg` | `0.75rem` | カード等の大きめサーフェスの角丸 |

## 運用ルール

- 角丸は直接記述せず、必ずトークン経由で参照する。
